import { FixsearchpannelDirective } from './fixsearchpannel.directive';

describe('FixsearchpannelDirective', () => {
  it('should create an instance', () => {
    const directive = new FixsearchpannelDirective();
    expect(directive).toBeTruthy();
  });
});
