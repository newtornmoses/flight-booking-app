import { ScrollheaderDirective } from './scrollheader.directive';

describe('ScrollheaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollheaderDirective();
    expect(directive).toBeTruthy();
  });
});
